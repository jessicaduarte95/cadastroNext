import styled from 'styled-components';
import { TextColors } from '../../style/theme';

export const Title = styled.p`
    font-size: 24px;
    font-weight: 500;
    color: ${TextColors.Title};
`;

export const SubTitle = styled.p`
    font-size: 20px;
    font-weight: 500;
    line-height: 20px;
    color: ${TextColors.Title};
`;